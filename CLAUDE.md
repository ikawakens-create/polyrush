# CLAUDE.md — Claude Code 向けプロジェクト指示書

このファイルは Claude Code (Code Web / CLI) がタスク着手前に読む指示書です。

## 作業開始時の環境チェック

1. `git log --oneline -5` でブランチ・最新コミットを確認する
2. `docs/handoff/LATEST.md` が存在する場合は読み、直近の文脈を把握する
3. 指示書の内容と現在のリポジトリ状態を照合してから着手する

## 📋 タスク完了時の報告フォーマット

完了報告は以下の 6 項目を含める。回答全体を 1 つのコードブロックにまとめ、
入れ子のコードフェンスを使わず、ファイル区切りは `===== ファイル名 =====` で示す。

1. **環境チェック結果** — `git log --oneline -5` の出力
2. **作成・変更ファイル一覧** — 新規/変更を明示。コード全文は書かない
3. **削除・変更した既存ファイルと理由**
4. **テスト結果** — 件数、pass/fail、所要時間。ドキュメントのみの場合はその旨
5. **指示書からの逸脱**
6. **次セッションへの申し送り** — 未解決の点、注意事項、次にやるべきこと

### handoff ファイルの更新(ADR-0016・必須)

タスク完了時、上記 1〜6 の報告に「次セッションへの申し送り」を加えた
内容で docs/handoff/LATEST.md を上書きし、同じ PR に含めること。
新しいファイルは作らず、常に LATEST.md の 1 ファイルを上書きする
(過去分は git 履歴で参照できる)。コードの全文は書かない。
また、新タスク開始時は環境チェックの後に docs/handoff/LATEST.md を
読み、直近の文脈を把握してから着手すること。
